"use client";

import PageLoader from "@/components/loaders/PageLoaderleanq_support_coordinator";
import withAuth from "@/lib/withAuthleanq_support_coordinator";

export default withAuth(() => <PageLoader />);
